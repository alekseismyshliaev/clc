{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "build:ui": "shx rm -rf build/* && cd client && npm run build && shx cp -r build ../",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "morgan": "^1.10.0",
    "node-fetch": "^3.2.0"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
